# llmstack runtime dependencies
#
# Install into the project venv:
#   python3 -m venv llmstack/.venv
#   llmstack/.venv/bin/pip install -r llmstack/requirements.txt
#
# Re-pin the floor versions whenever you intentionally upgrade (see UPGRADING.md
# > "Upgrading the Python toolchain").

# --- router (src/router.py) -------------------------------------------------
fastapi>=0.110,<1.0
httpx>=0.27,<1.0
uvicorn[standard]>=0.30,<1.0       # pulls uvloop, httptools, watchfiles, websockets

# --- check-models.sh helper -------------------------------------------------
huggingface_hub>=1.0,<2.0
PyYAML>=6.0,<7.0
hf_transfer>=0.1,<1.0              # accelerated GGUF downloads (HF_HUB_ENABLE_HF_TRANSFER=1)

# --- src/gen_opencode_config.py + scripts/install.sh -----------------------
# stdlib only (configparser, json, pathlib, re) - no entries here.
